'use strict';

/**
 * scientific-publication service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::scientific-publication.scientific-publication');
