{
  "collectionName": "components_shared_links",
  "info": {
    "displayName": "Link",
    "icon": "link",
    "description": "Reusable link with URL and target options"
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string"
    },
    "url": {
      "type": "string"
    },
    "target": {
      "type": "enumeration",
      "enum": ["_self", "_blank"],
      "default": "_self"
    },
    "linkType": {
      "type": "enumeration",
      "enum": ["url", "story", "email", "phone"],
      "default": "url"
    },
    "storyPath": {
      "type": "string"
    }
  }
}
